#Double gradient generator

A simple way to generate a gradient out of two colors both as a background for an entire element or a text.

See the demo: https://anjavm.github.io/doublegradientgenerator/
